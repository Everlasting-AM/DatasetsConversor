from setuptools import setup, find_packages

setup(
    name='MiProyecto',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # Aquí se podrían agregar dependencias externas necesarias
    ]
)